bootstrap: sudo apt-get update; sudo apt-get install -y patchelf python3-jinja2 python3-dev python3-pip python3-numpy python3-protobuf; mkdir -p build; cd build;cmake -DCMAKE_EXPORT_COMPILE_COMMANDS=ON .. || true
